<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- As the main content view, the view below consumes the entire
			space available using match_parent in both dimensions. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:baselineAligned="false"
        android:orientation="horizontal">
        <!--
        android:showDividers="middle"
        android:divider="?android:dividerVertical" >
        -->

        <!--<fragment-->
        <!--android:id="@+id/item_folder_list"-->
        <!--android:name="com.qnap.qfilehd.activity.nasfilelist.ItemFolderListFragment"-->
        <!--android:layout_width="0dp"-->
        <!--android:layout_height="match_parent"-->
        <!--android:layout_weight="3" />-->

        <FrameLayout
            android:id="@+id/item_folder_container"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="7"></FrameLayout>

    </LinearLayout>

    <include
        android:id="@+id/file_details_drawer_container"
        layout="@layout/file_details_view"
        android:layout_width="@dimen/left_drawer_container_width"
        android:layout_height="match_parent"
        android:layout_gravity="end" />

</android.support.v4.widget.DrawerLayout>